language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script:
  - nosetests tests/test_load_params.py
  - nosetests tests/test_dataset.py
  - nosetests tests/test_NMT.py